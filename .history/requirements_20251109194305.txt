# Requirements for Smart Cities DA-2 Project Only
# Minimal dependencies for EV Navigation & Station Placement Visualization
#
# FOR HARDWARE DEMO ONLY (Raspberry Pi):
#   Use: pip install -r requirements-hardware.txt
#   This installs only RPi.GPIO, OLED display, and related hardware libraries
#
# FOR FULL SYSTEM (with visualization):
#   Use: pip install -r requirements.txt

# Core data processing
numpy>=1.21.0
pandas>=1.5.0

# Visualization
matplotlib>=3.5.0
folium>=0.12.0

# Optional: For better map backgrounds (requires GDAL system libraries)
# On Raspberry Pi: sudo apt-get install gdal-bin libgdal-dev python3-gdal
# Then: pip install contextily>=1.4.0
# contextily>=1.4.0

# Geospatial (if using gridding module)
geopandas>=0.11.0
shapely>=1.8.0
osmnx>=1.2.0

# Hardware (only needed on Raspberry Pi)
# RPi.GPIO>=0.7.0
# adafruit-circuitpython-ssd1306>=2.12.0
# pillow>=9.0.0


